// GitHub API Configuration Template
// Rename this file to config.js and add your GitHub token
const config = {
    github: {
        token: 'YOUR_GITHUB_TOKEN_HERE' // Replace with your personal access token
    }
};

export default config;
